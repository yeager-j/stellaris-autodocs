# Original work; no Stellaris file is reproduced.
#
# `tech_collision_consumer` reads the cross-source-contested symbol, `@shared_symbol`;
# `tech_clean_consumer` reads `@base_cost`, which only Vanilla declares here. Under `r19`'s
# first-wins rule both resolve, and the contested read's provenance must name Vanilla's
# winning declaration rather than the Target Mod's rejected one.

tech_collision_consumer = {
	cost = @shared_symbol
	tier = 1
}

tech_clean_consumer = {
	cost = @base_cost
	tier = 1
}
