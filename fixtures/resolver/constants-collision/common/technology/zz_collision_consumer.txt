# Original work; no Stellaris file is reproduced.
#
# `tech_collision_consumer` reads the colliding symbol, `@shared_symbol`; `tech_clean_consumer`
# reads `@base_cost`, which only Vanilla declares here and so is not contested. The pair is
# what proves the per-symbol marking: one consumer is pending, the other still resolves,
# from the same corpus and the same technologies row.

tech_collision_consumer = {
	cost = @shared_symbol
	tier = 1
}

tech_clean_consumer = {
	cost = @base_cost
	tier = 1
}
