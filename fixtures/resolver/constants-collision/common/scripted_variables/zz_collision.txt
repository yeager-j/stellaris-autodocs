# Original work; no Stellaris file is reproduced.
#
# Redeclares `registration-vanilla/`'s `@shared_symbol` from a late-sorting file, which makes
# this the losing side of a cross-source repeat: `r19` measured that the first declaration in
# the one global path order wins, so Vanilla's 5 survives and this declaration is recorded as
# a rejected duplicate.

@shared_symbol = 99
